---
- hosts: 127.0.0.1
  connection: local
  sudo: true
  tasks:
    - name: cleaning up bash history
      command: "{{ item }}"
      sudo: false
      with_items:
        - "history -c"
        - "history -w"

    - name: rebooting host
      command: shutdown -r now "rebooting to complete cleanup"
